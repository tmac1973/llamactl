services:
  llama-toolchest:
    build:
      context: .
      dockerfile: Dockerfile.cuda
    container_name: llama-toolchest
    ports:
      - "${LLAMA_TOOLCHEST_PORT:-3000}:3000"   # llama-toolchest management UI
      - "${LLAMA_TOOLCHEST_INFERENCE_PORT:-8080}:8080"   # llama-server router (chat UI + inference)
    volumes:
      - llama-toolchest-data:/data:z
    env_file:
      - path: .env
        required: false
    # Docker uses deploy.resources for GPU access
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    # Podman uses CDI device strings (ignored by Docker)
    devices:
      - nvidia.com/gpu=all
    restart: unless-stopped

volumes:
  llama-toolchest-data:
    name: llama-toolchest-data
    driver: local
